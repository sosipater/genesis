# Genesis (mobile)

Flutter Android (and other targets) client for the Genesis monorepo. See repo root `README.md` and `CONTEXT.md`.

From repo root, use `.\mobile.ps1 get|run|test` or work in this directory with standard Flutter commands.
